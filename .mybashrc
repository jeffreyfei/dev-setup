# User specific aliases and functions
alias clearall='tput reset'


if type vimx >> /dev/null; then
    alias vim='vimx'
fi

# i3 Specific
alias resetwifi='killall nm-applet; nohup nm-applet &'
export PATH="$HOME/bin:$PATH"
